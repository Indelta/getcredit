import React from 'react';
import ContactsInner from '../components/Contacts';

const Contacts = () => {
    return (
        <section id="contacts">
            <ContactsInner />
        </section>
    );
}

export default Contacts;